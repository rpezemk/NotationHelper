<Window x:Class="NAudioTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NAudioTest"
        xmlns:views="clr-namespace:NAudioTest.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" Content="ShowSequence" Click="ShowSequence"/>
        <Button Grid.Column="1" Content="PlaySequence" Click="PlaySequence"/>

        <views:RhythmSequenceView x:Name="SequenceView" Grid.Row="1" Grid.ColumnSpan="3"/>
        <ScrollViewer Grid.Row="2" Grid.ColumnSpan="3">
            <TextBox x:Name="LoggerTextBox" Background="DarkBlue" Foreground="Yellow" IsReadOnly="True" FontFamily="Cascadia Mono"/>
        </ScrollViewer>
    </Grid>
</Window>
